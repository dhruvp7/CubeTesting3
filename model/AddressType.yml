# Split from UtilityAIOntology.cube.yml - see Cube_Format_Changes_Needed.md
# for the full list of open items (BigQuery sql_table qualification, join FK
# verification, placeholder primary keys) that still need review for this cube.
cubes:
  - name: AddressType
    title: Address Type
    description: List ofaddress types
    sql_table: eng_ao_format_testing.address_type
    dimensions:
      - name: description
        sql: "{CUBE}.description"
        type: string
        title: Description
        meta:
          llm_description: A textual explanation of the address type.
      - name: id
        sql: "{CUBE}.id"
        type: string
        primary_key: true
        title: ID
        meta:
          llm_description: A unique identifier for the address type.
      - name: name
        sql: "{CUBE}.name"
        type: string
        title: Name
        meta:
          llm_description: The name of the address type.
    measures:
      - name: count
        type: count
    meta:
      uid: com::apporchid::utilityai::AddressType
      llm_description: The AddressType table categorizes different types of addresses, providing a description, unique identifier, and name for each address type.
