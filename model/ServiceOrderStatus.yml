# Split from UtilityAIOntology.cube.yml - see Cube_Format_Changes_Needed.md
# for the full list of open items (BigQuery sql_table qualification, join FK
# verification, placeholder primary keys) that still need review for this cube.
cubes:
  - name: ServiceOrderStatus
    title: Service Order Status
    sql_table: eng_ao_format_testing.service_order_status
    dimensions:
      - name: description
        sql: "{CUBE}.description"
        type: string
        title: Description
        meta:
          llm_description: A textual explanation of the service order status.
      - name: statusCode
        sql: "{CUBE}.status_code"
        type: string
        primary_key: true
        title: Status Code
        meta:
          llm_description: A unique code representing the specific status of a service order.
    measures:
      - name: count
        type: count
    meta:
      uid: com::apporchid::utilityai::ServiceOrderStatus
      llm_description: ServiceOrderStatus table captures the status details of service orders, including a textual description and a unique status code, facilitating the tracking and management of service order states.
