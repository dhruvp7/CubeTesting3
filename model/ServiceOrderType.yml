# Split from UtilityAIOntology.cube.yml - see Cube_Format_Changes_Needed.md
# for the full list of open items (BigQuery sql_table qualification, join FK
# verification, placeholder primary keys) that still need review for this cube.
cubes:
  - name: ServiceOrderType
    title: Service Order Type
    sql_table: eng_ao_format_testing.ServiceOrderType
    dimensions:
      - name: description
        sql: "{CUBE}.description"
        type: string
        title: Description
        meta:
          llm_description: A detailed explanation of the service order type.
      - name: typeId
        sql: "{CUBE}.type_id"
        type: string
        title: Type Id
        meta:
          llm_description: A unique identifier for the service order type.
    measures:
      - name: count
        type: count
    meta:
      uid: com::apporchid::utilityai::ServiceOrderType
      llm_description: ServiceOrderType table categorizes different types of service orders with unique identifiers and descriptions, facilitating the management and tracking of various service-related tasks.
