# Split from UtilityAIOntology.cube.yml - see Cube_Format_Changes_Needed.md
# for the full list of open items (BigQuery sql_table qualification, join FK
# verification, placeholder primary keys) that still need review for this cube.
cubes:
  - name: MeterInstallation
    title: Meter Installation
    description: Specifies the meter installed in a premise
    sql_table: eng_ao_format_testing.MeterInstallation
    joins:
      # TODO - VERIFY: references nested/dotted field 'meter.id' instead of a flat column. Confirm the real FK column name in the warehouse table and update this SQL condition if needed.
      - name: Meter
        relationship: many_to_one
        sql: "{CUBE}.meter.id = {Meter}.id"
      # TODO - VERIFY: references nested/dotted field 'premise.id' instead of a flat column. Confirm the real FK column name in the warehouse table and update this SQL condition if needed.
      - name: Premise
        relationship: many_to_one
        sql: "{CUBE}.premise.id = {Premise}.id"
      # TODO - VERIFY: references nested/dotted field 'serviceType.id' instead of a flat column. Confirm the real FK column name in the warehouse table and update this SQL condition if needed.
      - name: ServiceType
        relationship: many_to_one
        sql: "{CUBE}.serviceType.id = {ServiceType}.id"
    dimensions:
      - name: endDate
        sql: "{CUBE}.end_date"
        type: time
        title: End Date
        meta:
          llm_description: The date when the meter installation ended.
      - name: id
        sql: "{CUBE}.installation_id"
        type: string
        primary_key: true
        title: ID
        meta:
          llm_description: A unique identifier for the meter installation record.
      - name: startDate
        sql: "{CUBE}.start_date"
        type: time
        title: Start Date
        meta:
          llm_description: The date when the meter installation started.
          words:
            - word: Starting
              wordType: word
              synonyms: []
              plural:
              abbreviation:
              alternateWords: []
              weight: 1
    measures:
      - name: count
        type: count
    meta:
      uid: com::apporchid::utilityai::MeterInstallation
      llm_description: The MeterInstallation table records the installation details of water meters, including start and end dates, meter ID, premise ID, and service type ID.
      words:
        - word: installation
          wordType: word
          synonyms: []
          plural: installations
          abbreviation:
          weight: 1
