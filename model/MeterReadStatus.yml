# Split from UtilityAIOntology.cube.yml - see Cube_Format_Changes_Needed.md
# for the full list of open items (BigQuery sql_table qualification, join FK
# verification, placeholder primary keys) that still need review for this cube.
cubes:
  - name: MeterReadStatus
    title: Meter Read Status
    description: List of meter read status
    sql_table: eng_ao_format_testing.meter_read_status
    dimensions:
      - name: description
        sql: "{CUBE}.description"
        type: string
        title: Description
        meta:
          llm_description: A detailed explanation or information about the meter read status.
      - name: id
        sql: "{CUBE}.id"
        type: string
        primary_key: true
        title: ID
        meta:
          llm_description: A unique identifier for each meter read status.
      - name: name
        sql: "{CUBE}.name"
        type: string
        title: Name
        meta:
          llm_description: The name or title of the meter read status.
    measures:
      - name: count
        type: count
    meta:
      uid: com::apporchid::utilityai::MeterReadStatus
      llm_description: The MeterReadStatus table captures the status of meter readings, including a description, unique identifier, and name for each status, facilitating the tracking and management of meter read operations.
      words:
        - word: read status
          wordType: word
          synonyms: []
          plural:
          abbreviation:
          weight: 1
