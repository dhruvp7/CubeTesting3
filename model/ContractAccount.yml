# Split from UtilityAIOntology.cube.yml - see Cube_Format_Changes_Needed.md
# for the full list of open items (BigQuery sql_table qualification, join FK
# verification, placeholder primary keys) that still need review for this cube.
cubes:
  - name: ContractAccount
    title: Contract Account
    description: Contract Account for a service
    sql_table: eng_ao_format_testing.ContractAccount
    joins:
      # TODO - VERIFY: references nested/dotted field 'customer.id' instead of a flat column. Confirm the real FK column name in the warehouse table and update this SQL condition if needed.
      - name: Customer
        relationship: many_to_one
        sql: "{CUBE}.customer.id = {Customer}.id"
      # TODO - VERIFY: references nested/dotted field 'installation.id' instead of a flat column. Confirm the real FK column name in the warehouse table and update this SQL condition if needed.
      - name: MeterInstallation
        relationship: many_to_one
        sql: "{CUBE}.installation.id = {MeterInstallation}.id"
    dimensions:
      - name: companyCode
        sql: "{CUBE}.company_code"
        type: string
        title: Company Code
        meta:
          llm_description: The unique code representing the company managing the contract.
      - name: connectionStatus
        sql: "{CUBE}.connection_status"
        type: string
        title: Connection status
        meta:
          llm_description: The current status of the connection associated with the contract.
      - name: endDate
        sql: "{CUBE}.end_date"
        type: time
        title: End Date
        meta:
          llm_description: The date when the contract ends.
      - name: id
        sql: "{CUBE}.id"
        type: string
        primary_key: true
        title: ID
        meta:
          llm_description: The unique identifier for the contract account.
      - name: isAutopayEnabled
        sql: "{CUBE}.is_autopay_enabled"
        type: boolean
        title: is Auto Pay enabled
        meta:
          llm_description: Indicates whether autopay is enabled for the contract.
      - name: isBudgetBillEnabled
        sql: "{CUBE}.is_budget_bill_enabled"
        type: boolean
        title: is Budget Bill enabled
        meta:
          llm_description: Indicates whether budget billing is enabled for the contract.
      - name: isEbillEnabled
        sql: "{CUBE}.is_ebill_enabled"
        type: boolean
        title: is E-Bill enabled
        meta:
          llm_description: Indicates whether e-billing is enabled for the contract.
      - name: isInstallmentPlanEnabled
        sql: "{CUBE}.is_installment_plan_enabled"
        type: boolean
        title: is Installment plan enabled
        meta:
          llm_description: Indicates whether an installment plan is enabled for the contract.
      - name: startDate
        sql: "{CUBE}.start_date"
        type: time
        title: Start Date
        meta:
          llm_description: The date when the contract starts.
    measures:
      - name: count
        type: count
    meta:
      uid: com::apporchid::utilityai::ContractAccount
      llm_description: The ContractAccount table manages customer contract details, including company code, connection status, customer and installation IDs, billing preferences, and contract start and end dates.
      words:
        - word: contract account
          wordType: word
          synonyms: []
          plural: contract accounts
          abbreviation:
          weight: 1
