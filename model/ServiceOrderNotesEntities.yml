# Split from UtilityAIOntology.cube.yml - see Cube_Format_Changes_Needed.md
# for the full list of open items (BigQuery sql_table qualification, join FK
# verification, placeholder primary keys) that still need review for this cube.
cubes:
  - name: ServiceOrderNotesEntities
    title: Service Order Notes Entities
    sql_table: eng_ao_format_testing.ServiceOrderNotesEntities
    dimensions: []
    measures:
      - name: count
        type: count
    meta:
      uid: com::apporchid::utilityai::ServiceOrderNotesEntities
      llm_description: ServiceOrderNotesEntities table captures detailed notes and metrics related to service orders, including zero-read months, sentiment scores, and reasons for zero reads, facilitating comprehensive tracking and analysis of service order issues.
