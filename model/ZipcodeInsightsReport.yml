# Split from UtilityAIOntology.cube.yml - see Cube_Format_Changes_Needed.md
# for the full list of open items (BigQuery sql_table qualification, join FK
# verification, placeholder primary keys) that still need review for this cube.
cubes:
  - name: ZipcodeInsightsReport
    title: Zipcode Insights Report
    description: Zipcode Insights report
    sql_table: eng_ao_format_testing.ZipcodeInsightsReport
    dimensions:
      - name: value
        sql: "{CUBE}.value"
        type: number
        title: Value
      - name: zipcode
        sql: "{CUBE}.zipcode"
        type: string
        title: Zipcode
    measures:
      - name: count
        type: count
    meta:
      uid: com::apporchid::customer::ui::insights::ZipcodeInsightsReport
