# Split from UtilityAIOntology.cube.yml - see Cube_Format_Changes_Needed.md
# for the full list of open items (BigQuery sql_table qualification, join FK
# verification, placeholder primary keys) that still need review for this cube.
cubes:
  - name: CommunicationMaster
    title: Communication Master
    description: Communication Master
    sql_table: eng_ao_format_testing.CommunicationMaster
    dimensions:
      - name: groupId
        sql: "{CUBE}.group_id"
        type: number
        title: Group ID
        meta:
          llm_description: A unique identifier for the communication group.
      - name: id
        sql: "{CUBE}.id"
        type: number
        primary_key: true
        title: ID
        meta:
          llm_description: A unique identifier for each communication entry.
      - name: name
        sql: "{CUBE}.name"
        type: string
        title: Name
        meta:
          llm_description: The name or title of the communication group.
    measures:
      - name: count
        type: count
    meta:
      uid: com::apporchid::utilityai::CommunicationMaster
      llm_description: CommunicationMaster table stores information about communication groups, including group IDs, individual communication IDs, and their respective names.
